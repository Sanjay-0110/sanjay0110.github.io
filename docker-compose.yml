services:
  portfolio:
    build: .
    ports:
      - "3000:3000"
    volumes:
      - .:/app
      - /app/node_modules   # keep container's node_modules, don't overwrite with host
    environment:
      - WATCHPACK_POLLING=true   # needed for hot reload inside Docker on Windows/Mac
    restart: unless-stopped